[project]
name = "dictdb"
version = "0.1.0"
description = "A Python package to manipulate dictionaries like databases."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "ruff>=0.11.2",
]
